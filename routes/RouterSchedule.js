/*var ScheduleCtrl;//=require('../controller/schedule.server.controller');
var fs = require('fs');
module.exports = function (app) {
  app.get('/schedule/find', ScheduleCtrl.activities);
  app.post('/schedule/save', ScheduleCtrl.save);

};*/
